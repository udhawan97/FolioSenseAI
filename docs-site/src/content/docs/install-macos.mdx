---
title: Install on macOS
description: Install the FolioOrb .dmg on Apple Silicon, clear the Gatekeeper warning, and find where your data lives.
---

import { Steps, Aside } from '@astrojs/starlight/components';

The macOS download is a standard `.dmg` for **Apple Silicon** (M1 and newer), targeting
macOS 11 Big Sur and later.

<Steps>

1. Download `FolioOrb-macOS-arm64-v5.6.0.dmg` from the
   [latest release](https://github.com/udhawan97/FolioOrb/releases/latest).

2. Open the `.dmg` and drag **FolioOrb** into **Applications**.

3. Launch it from Applications or Spotlight.

</Steps>

## First launch: the Gatekeeper warning

Because early builds aren't notarized, macOS blocks the first launch with a message like
*"FolioOrb can't be opened because Apple cannot check it for malicious software."*
This is expected. To allow it:

<Steps>

1. Try to open the app once (double-click). Dismiss the warning dialog.

2. Open **System Settings → Privacy &amp; Security**.

3. Scroll down to the message about FolioOrb being blocked and click **Open Anyway**.

4. Confirm with **Open**. macOS remembers the choice — future launches are one click.

</Steps>

<Aside type="note" title="Why the warning?">
It means the app isn't signed with a paid Apple Developer certificate — not that anything
is wrong with it. You can [verify your download](/FolioOrb/download/#verify-your-download)
against the published `SHA256SUMS.txt` for extra confidence. Signing and notarization are on
the [roadmap](/FolioOrb/releases-and-versioning/#code-signing-roadmap).
</Aside>

## Where your data lives

| What | Location |
| --- | --- |
| Portfolio database | `~/Library/Application Support/FolioOrb/database/` |
| Config (`.env`) | `~/Library/Application Support/FolioOrb/.env` |

The app never writes inside `/Applications`, so updates and reinstalls leave your data
untouched. See [Updating](/FolioOrb/updating/).

## Uninstall

Drag **FolioOrb** from Applications to the Trash. To also remove your data, delete
`~/Library/Application Support/FolioOrb`.

## Intel Macs

An Intel-native download isn't published yet. On an Intel Mac, use the
[install script or build from source](/FolioOrb/build-from-source/) — both run fine
with Python 3.11+.
