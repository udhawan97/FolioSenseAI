# Desktop packaging dependencies — kept separate from requirements.txt so the
# web/dev/CI path and dependency audit scope stay unchanged. Install alongside
# requirements.txt only when building the DMG/EXE:
#   pip install -r requirements.txt -r requirements-desktop.txt
pyinstaller>=6.11,<7
pywebview>=5.3,<6
